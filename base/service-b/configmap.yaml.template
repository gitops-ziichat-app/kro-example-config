name: ${SERVICE_NAME}
port: ${SERVICE_PORT}

store:
  mongo:
    data_user_profile:
      clientUrl: mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@${MONGO_HOST}:${MONGO_PORT}
      dbName: ${MONGO_DB_USER_PROFILE}